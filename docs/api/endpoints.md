# Internal API Endpoints

This document serves as a reference for internal routing and function calls within the app.py structure.

- `generate_image(prompt, **kwargs)`
- `upscale_image(image, factor=4)`
- `save_to_gallery(image_data, metadata)`
